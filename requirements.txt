# Use the PyTorch CUDA 12.1 wheels index:
--index-url https://download.pytorch.org/whl/cu121

# Core CUDA stack (exact builds that worked for you)
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Docling + friends
docling==2.57.0

# HF + generation stack
transformers>=4.46,<5
huggingface_hub>=0.25
accelerate
safetensors

# Imaging (compatible with docling’s constraint: <12.0.0)
pillow==11.2.0

# Markdown rendering for HTML exporters
markdown>=3.5

# (Optional) RapidOCR is pulled by docling; include for clarity
rapidocr>=1.3

# General utilities implicitly needed by the above (usually pulled automatically)
# numpy, pyyaml, requests, regex are dependencies of transformers/docling.
